{%- macro voila_setup(base_url, labextensions) -%}
<script src="{{ page_config['fullStaticUrl'] | e }}/bundle.js"></script>

<script>
  // TODO: load the JupyterLab prebuilt extensions.
  {% for ext in labextensions -%}
    console.log("Load the JupyterLab extension \"{{ ext }}\".");
  );
  {% endfor %}
</script>
{%- endmacro %}
